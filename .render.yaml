services:
  - type: web
    name: thrust-backend
    env: python
    rootDir: backend         # ✅ makes backend/ the service root
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host=0.0.0.0 --port=10000
